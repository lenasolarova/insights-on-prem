apiVersion: v1
kind: Namespace
metadata:
  name: open-cluster-management-insights
  labels:
    app: insights-on-prem
---
apiVersion: cluster.open-cluster-management.io/v1beta2
kind: ManagedClusterSetBinding
metadata:
  name: global
  namespace: open-cluster-management-insights
spec:
  clusterSet: global
---
# Pre-create the workload namespace so the pull secret can be applied
# before the addon ManifestWork deploys it.
apiVersion: v1
kind: Namespace
metadata:
  name: insights-on-prem
  labels:
    app: insights-on-prem
